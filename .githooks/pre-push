#!/usr/bin/env bash
set -euo pipefail

if [ "${SKIP_HOOKS:-0}" = "1" ]; then
  echo "SKIP_HOOKS=1 — skipping pre-push hook"
  exit 0
fi

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "$ROOT_DIR"

bash tests/run-core-suite.sh
exit $?
